import React from "react";
import Base from "../core/Base";

const UserDashboard = () =>{
    return(
        <Base title = "User Dashboard" description = "User Dashboard Page">
            <h2>Welcome to User Dashboard Page</h2>
        </Base>
    )
}

export default UserDashboard